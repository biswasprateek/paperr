# Auto detect text files and perform LF normalization
* text=auto

# Launcher scripts break if they get the wrong line endings on checkout.
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.sh text eol=lf
*.command text eol=lf
