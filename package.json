{
  "name": "paperr",
  "private": true,
  "version": "1.0.0-beta",
  "description": "Self-hosted household OS — tasks, calendar, notes, and a local AI assistant, all LAN-only",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd .. && npm run setup:env",
    "setup:env": "node scripts/setup-env.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "playwright": "^1.61.1"
  }
}
